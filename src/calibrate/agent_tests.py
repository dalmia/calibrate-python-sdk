"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from calibrate import errors, models, utils
from calibrate._hooks import HookContext
from calibrate.types import OptionalNullable, UNSET
from calibrate.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Iterable, List, Mapping, Optional, Union


class AgentTests(BaseSDK):
    def run(
        self,
        *,
        agent_uuid: str,
        x_api_key: OptionalNullable[str] = UNSET,
        x_org_uuid: OptionalNullable[str] = UNSET,
        test_uuids: OptionalNullable[Iterable[str]] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TaskCreateResponse:
        r"""Run Agent Test

        Run one or more tests for an agent.

        This starts a background task that runs the calibrate LLM tests command
        with the agent's config and the combined test cases from all specified tests.

        Returns a task ID that can be used to poll for status and results.

        Auth: requires either a JWT (frontend) or an `sk_` API key. The agent
        must belong to the caller's org or this 404s.

        :param agent_uuid:
        :param x_api_key:
        :param x_org_uuid:
        :param test_uuids:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.RunAgentTestAgentTestsAgentAgentUUIDRunPostRequest(
            agent_uuid=agent_uuid,
            x_api_key=x_api_key,
            x_org_uuid=x_org_uuid,
            body=models.RunTestRequest(
                test_uuids=utils.unmarshal(test_uuids, OptionalNullable[List[str]]),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agent-tests/agent/{agent_uuid}/run",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models.RunTestRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="run_agent_test_agent_tests_agent__agent_uuid__run_post",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["agent-tests"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TaskCreateResponse, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                errors.HTTPValidationErrorData, http_res
            )
            raise errors.HTTPValidationError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.CalibrateDefaultError("Unexpected response received", http_res)

    async def run_async(
        self,
        *,
        agent_uuid: str,
        x_api_key: OptionalNullable[str] = UNSET,
        x_org_uuid: OptionalNullable[str] = UNSET,
        test_uuids: OptionalNullable[Iterable[str]] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TaskCreateResponse:
        r"""Run Agent Test

        Run one or more tests for an agent.

        This starts a background task that runs the calibrate LLM tests command
        with the agent's config and the combined test cases from all specified tests.

        Returns a task ID that can be used to poll for status and results.

        Auth: requires either a JWT (frontend) or an `sk_` API key. The agent
        must belong to the caller's org or this 404s.

        :param agent_uuid:
        :param x_api_key:
        :param x_org_uuid:
        :param test_uuids:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.RunAgentTestAgentTestsAgentAgentUUIDRunPostRequest(
            agent_uuid=agent_uuid,
            x_api_key=x_api_key,
            x_org_uuid=x_org_uuid,
            body=models.RunTestRequest(
                test_uuids=utils.unmarshal(test_uuids, OptionalNullable[List[str]]),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agent-tests/agent/{agent_uuid}/run",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models.RunTestRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="run_agent_test_agent_tests_agent__agent_uuid__run_post",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["agent-tests"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TaskCreateResponse, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                errors.HTTPValidationErrorData, http_res
            )
            raise errors.HTTPValidationError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.CalibrateDefaultError("Unexpected response received", http_res)

    def run_batch(
        self,
        *,
        x_api_key: OptionalNullable[str] = UNSET,
        x_org_uuid: OptionalNullable[str] = UNSET,
        body: OptionalNullable[
            Union[models.BatchRunRequest, models.BatchRunRequestTypedDict]
        ] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.BatchTestRunResponse:
        r"""Run Tests Batch

        Run every linked test for a set of agents, one ``llm-unit-test`` job per agent.

        Scope is driven by the optional ``agent_names`` payload:

        - **Provided (non-empty)** — run only those agents. Names are unique per org
        and **all are validated up front**: if any doesn't resolve to a
        (non-deleted) agent in the caller's org, the call 404s with the offending
        names and NO jobs are created.
        - **Omitted / null / empty** — run every agent in the caller's org.

        For each selected agent, its linked tests are launched as one job. Agents
        with no linked tests or an unverified connection are reported under
        ``skipped`` instead of failing the batch. Subject to the normal per-org
        concurrency queue, so over-limit jobs come back ``queued``.

        Auth accepts a JWT (frontend) or an `sk_` API key (programmatic clients).
        Returns one ``runs`` entry per launched agent with ``agent_name``,
        ``agent_uuid``, ``task_id``, and ``status``.

        :param x_api_key:
        :param x_org_uuid:
        :param body:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.RunTestsBatchAgentTestsRunPostRequest(
            x_api_key=x_api_key,
            x_org_uuid=x_org_uuid,
            body=utils.get_pydantic_model(
                body, OptionalNullable[models.BatchRunRequest]
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agent-tests/run",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                True,
                True,
                "json",
                OptionalNullable[models.BatchRunRequest],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="run_tests_batch_agent_tests_run_post",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["agent-tests"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.BatchTestRunResponse, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                errors.HTTPValidationErrorData, http_res
            )
            raise errors.HTTPValidationError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.CalibrateDefaultError("Unexpected response received", http_res)

    async def run_batch_async(
        self,
        *,
        x_api_key: OptionalNullable[str] = UNSET,
        x_org_uuid: OptionalNullable[str] = UNSET,
        body: OptionalNullable[
            Union[models.BatchRunRequest, models.BatchRunRequestTypedDict]
        ] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.BatchTestRunResponse:
        r"""Run Tests Batch

        Run every linked test for a set of agents, one ``llm-unit-test`` job per agent.

        Scope is driven by the optional ``agent_names`` payload:

        - **Provided (non-empty)** — run only those agents. Names are unique per org
        and **all are validated up front**: if any doesn't resolve to a
        (non-deleted) agent in the caller's org, the call 404s with the offending
        names and NO jobs are created.
        - **Omitted / null / empty** — run every agent in the caller's org.

        For each selected agent, its linked tests are launched as one job. Agents
        with no linked tests or an unverified connection are reported under
        ``skipped`` instead of failing the batch. Subject to the normal per-org
        concurrency queue, so over-limit jobs come back ``queued``.

        Auth accepts a JWT (frontend) or an `sk_` API key (programmatic clients).
        Returns one ``runs`` entry per launched agent with ``agent_name``,
        ``agent_uuid``, ``task_id``, and ``status``.

        :param x_api_key:
        :param x_org_uuid:
        :param body:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.RunTestsBatchAgentTestsRunPostRequest(
            x_api_key=x_api_key,
            x_org_uuid=x_org_uuid,
            body=utils.get_pydantic_model(
                body, OptionalNullable[models.BatchRunRequest]
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agent-tests/run",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                True,
                True,
                "json",
                OptionalNullable[models.BatchRunRequest],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="run_tests_batch_agent_tests_run_post",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["agent-tests"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.BatchTestRunResponse, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                errors.HTTPValidationErrorData, http_res
            )
            raise errors.HTTPValidationError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.CalibrateDefaultError("Unexpected response received", http_res)

    def get_run(
        self,
        *,
        task_id: str,
        x_api_key: OptionalNullable[str] = UNSET,
        x_org_uuid: OptionalNullable[str] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TestRunStatusResponse:
        r"""Get Agent Test Run Status

        Get the status of an agent test run.

        Requires either a JWT (frontend) or an `sk_` API key, plus org
        ownership of the run. Unauthenticated access to a completed run is only
        possible once it is made public, via the share-token endpoint in the public
        router.

        Returns the current status and, if done, the test results.

        :param task_id:
        :param x_api_key:
        :param x_org_uuid:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetAgentTestRunStatusAgentTestsRunTaskIDGetRequest(
            task_id=task_id,
            x_api_key=x_api_key,
            x_org_uuid=x_org_uuid,
        )

        req = self._build_request(
            method="GET",
            path="/agent-tests/run/{task_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get_agent_test_run_status_agent_tests_run__task_id__get",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["agent-tests"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TestRunStatusResponse, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                errors.HTTPValidationErrorData, http_res
            )
            raise errors.HTTPValidationError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.CalibrateDefaultError("Unexpected response received", http_res)

    async def get_run_async(
        self,
        *,
        task_id: str,
        x_api_key: OptionalNullable[str] = UNSET,
        x_org_uuid: OptionalNullable[str] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TestRunStatusResponse:
        r"""Get Agent Test Run Status

        Get the status of an agent test run.

        Requires either a JWT (frontend) or an `sk_` API key, plus org
        ownership of the run. Unauthenticated access to a completed run is only
        possible once it is made public, via the share-token endpoint in the public
        router.

        Returns the current status and, if done, the test results.

        :param task_id:
        :param x_api_key:
        :param x_org_uuid:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetAgentTestRunStatusAgentTestsRunTaskIDGetRequest(
            task_id=task_id,
            x_api_key=x_api_key,
            x_org_uuid=x_org_uuid,
        )

        req = self._build_request_async(
            method="GET",
            path="/agent-tests/run/{task_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get_agent_test_run_status_agent_tests_run__task_id__get",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["agent-tests"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TestRunStatusResponse, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                errors.HTTPValidationErrorData, http_res
            )
            raise errors.HTTPValidationError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.CalibrateDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.CalibrateDefaultError("Unexpected response received", http_res)
